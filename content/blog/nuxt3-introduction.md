---
title: 'Nuxt 3 入门指南'
description: 'Nuxt 3 是一个基于 Vue 3 的全栈框架，本文将介绍如何开始使用 Nuxt 3 构建应用。'
date: 2024-01-20
category: '前端开发'
readTime: 8
cover: 'https://images.unsplash.com/photo-1633356122544-f134324a6cee?w=800&h=400&fit=crop'
---

# Nuxt 3 入门指南

[Nuxt 3](https://nuxt.com/) 是一个基于 Vue 3 的全栈框架，提供了很多开箱即用的功能，让开发者能够快速构建现代化的 Web 应用。

## 什么是 Nuxt 3？

Nuxt 3 是 Nuxt.js 的最新版本，它基于 Vue 3 构建，提供了以下特性：

- 🚀 **服务端渲染 (SSR)** - 提升首屏加载速度和 SEO
- 💨 **静态站点生成 (SSG)** - 生成静态 HTML 文件
- 📦 **文件路由系统** - 自动生成路由
- 🎨 **组件自动导入** - 无需手动导入组件
- 🔌 **强大的模块系统** - 丰富的功能模块

## 开始使用

### 安装

使用以下命令创建一个新的 Nuxt 3 项目：

```bash
npx nuxi@latest init my-nuxt-app
```

### 项目结构

```
my-nuxt-app/
├── .nuxt/           # Nuxt 构建输出
├── assets/          # 静态资源
├── components/      # Vue 组件
├── composables/     # 组合式函数
├── layouts/         # 布局组件
├── middleware/      # 中间件
├── pages/           # 页面组件
├── plugins/         # 插件
├── public/          # 静态文件
├── server/          # 服务端代码
├── nuxt.config.ts   # Nuxt 配置文件
└── package.json
```

## 核心特性

### 1. 文件路由系统

在 `pages/` 目录下创建 Vue 文件，Nuxt 会自动生成对应的路由：

```bash
pages/
├── index.vue        # /
├── about.vue        # /about
└── blog/
    ├── index.vue    # /blog
    └── [slug].vue   # /blog/:slug
```

### 2. 组件自动导入

在 `components/` 目录下创建的组件会自动导入，无需手动 import：

```vue
<template>
  <div>
    <!-- components/TheHeader.vue 自动导入 -->
    <TheHeader />
  </div>
</template>
```

### 3. 组合式函数

使用 Nuxt 的组合式函数来访问框架功能：

```vue
<script setup>
// 获取路由
const route = useRoute()

// 获取数据
const { data } = await useFetch('/api/posts')

// 设置页面头部信息
useHead({
  title: '我的页面',
  meta: [
    { name: 'description', content: '页面描述' }
  ]
})
</script>
```

## 总结

Nuxt 3 是一个功能强大的框架，能够帮助开发者快速构建现代化的 Web 应用。如果你熟悉 Vue 3，那么学习 Nuxt 3 会非常容易。

希望这篇入门指南对你有所帮助！
